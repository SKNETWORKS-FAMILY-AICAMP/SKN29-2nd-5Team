# 📄 산출물 2: 인공지능 학습 결과서

## 1. 모델링 전략

### 1-1. 평가 지표 선정

본 프로젝트는 서울시 공공자전거 따릉이의 **대여소별·시간대별 대여량을 예측하는 회귀 문제**이다. 따라서 분류 문제에서 사용하는 Accuracy, Recall, F1-Score, ROC-AUC 대신 실제 대여량 예측 오차를 해석할 수 있는 회귀 평가 지표를 사용하였다.

| 지표 | 선정 이유 |
|------|----------|
| MAE | 실제 대여량과 예측 대여량이 평균적으로 몇 대 차이 나는지 직관적으로 확인할 수 있다. 운영자가 예측 오차를 가장 쉽게 이해할 수 있는 지표이다. |
| RMSE | 큰 오차에 더 큰 패널티를 부여한다. 출퇴근 시간대나 특정 대여소처럼 수요가 몰리는 구간의 큰 예측 실패를 민감하게 확인하기 위해 사용하였다. |
| WAPE | 전체 실제 대여량 대비 절대 오차의 비율을 나타낸다. 대여소별 수요 규모가 다르기 때문에 전체 수요 대비 오차 수준을 함께 판단하기 위해 사용하였다. |
| R2 | 모델이 실제 대여량 변동을 얼마나 설명하는지 확인하기 위한 지표이다. Baseline 대비 설명력 개선 여부를 판단하는 데 사용하였다. |

> 본 프로젝트에서는 특정 지표 하나만으로 모델을 선정하지 않았다. MAE, RMSE, WAPE, R2를 함께 비교하고, Baseline 대비 개선 정도와 실제 서비스 적용 가능성을 함께 고려하여 최종 모델을 선정하였다.

---

### 1-2. 후보 모델 선정

| 모델 | 유형 | 선정 근거 |
|------|------|----------|
| Baseline 모델 | 통계 기반 기준 모델 | 대여소·시간대·휴일 여부별 평균 및 과거 대여량을 활용하여 모델 성능 비교의 기준선을 설정하기 위해 사용하였다. |
| XGBoost | 머신러닝, Gradient Boosting | 정형 데이터와 비선형 관계 학습에 강하며, 대여량처럼 음수가 될 수 없는 count형 target에 적합한 Poisson 목적 함수를 적용할 수 있다. |
| LightGBM | 머신러닝, Gradient Boosting | 대용량 정형 데이터 학습 속도가 빠르고, XGBoost와 유사한 부스팅 계열 모델로 성능 비교에 적합하다. |
| GRU | 딥러닝, 순환신경망 | 시간 흐름을 sequence 형태로 학습할 수 있어, 직전 24시간의 대여량 흐름을 반영하는 시계열 대안 모델로 실험하였다. |

후보 모델은 단순히 다양한 알고리즘을 나열한 것이 아니라, 본 데이터의 특성에 맞춰 선정하였다. 최종 데이터는 대여소 정보, 시간 정보, 날씨 정보, 과거 대여량 Lag, Rolling, Diff 변수를 포함한 정형 시계열 데이터이므로 Tree 기반 부스팅 모델을 주요 후보로 설정하였다. GRU는 시간 순서 학습 가능성을 검증하기 위한 비교 모델로 사용하였다.

---

### 1-3. 실험 계획

- Baseline 모델을 먼저 구축하여 최소 기준 성능을 확인한다.
- XGBoost 기본 모델을 학습하여 Tree 기반 부스팅 모델의 기본 성능을 확인한다.
- XGBoost에 대해 learning rate, tree depth, 정규화, sampling 관련 하이퍼파라미터를 조정하여 튜닝 효과를 비교한다.
- LightGBM을 학습하여 XGBoost와 동일한 정형 데이터 조건에서 성능을 비교한다.
- GRU 모델을 학습하여 sequence 기반 딥러닝 모델이 Tree 기반 모델보다 유리한지 확인한다.
- 최종 모델은 Test 데이터 기준 MAE, RMSE, WAPE는 낮고 R2는 높은 모델을 우선 검토하되, Baseline 대비 개선율과 운영 적용 가능성까지 고려하여 선정한다.

---

## 2. 머신러닝 모델 학습 결과

### 2-1. Baseline 모델

Baseline은 학습 모델이 실제로 의미 있는 예측 성능을 내는지 판단하기 위한 기준선이다. 본 프로젝트에서는 단순 평균뿐 아니라 대여소, 시간대, 요일, 휴일 여부, 과거 동일 시간대 대여량을 활용한 여러 기준 모델을 비교하였다.

**Baseline 구성**

| Baseline 모델 | 설명 |
|---------------|------|
| 전체 평균 기반 모델 | 학습 데이터의 평균 대여량을 모든 시간대에 동일하게 예측 |
| 대여소 평균 기반 모델 | 각 대여소의 평균 대여량을 예측값으로 사용 |
| 대여소·시간대 평균 기반 모델 | 대여소별 시간대 평균 대여량을 예측값으로 사용 |
| 대여소·시간대·요일 평균 기반 모델 | 대여소, 시간대, 요일별 평균 대여량을 예측값으로 사용 |
| 대여소·시간대·휴일 여부 평균 기반 모델 | 대여소, 시간대, 휴일 여부별 평균 대여량을 예측값으로 사용 |
| Lag 기반 모델 | 1시간 전, 24시간 전, 168시간 전 대여량을 예측값으로 사용 |

**Baseline 성능 결과, Test 기준**

| 모델 | MAE | RMSE | WAPE | R2 |
|------|------:|------:|------:|------:|
| 대여소·시간대·휴일 여부 평균 | 1.2635 | 2.1827 | 0.7995 | 0.4791 |
| 168시간 전 대여량 | 1.2781 | 2.4573 | 0.8087 | 0.3398 |
| 대여소·시간대·요일 평균 | 1.2817 | 2.2268 | 0.8110 | 0.4579 |
| 1시간 전 대여량 | 1.2909 | 2.5065 | 0.8168 | 0.3131 |
| 24시간 전 대여량 | 1.3188 | 2.5752 | 0.8345 | 0.2749 |
| 대여소·시간대 평균 | 1.3376 | 2.3415 | 0.8464 | 0.4006 |
| 대여소 평균 | 1.6280 | 2.7500 | 1.0301 | 0.1732 |
| 전체 평균 | 1.8894 | 3.0398 | 1.1955 | -0.0103 |

**해석**

가장 성능이 좋은 Baseline은 대여소·시간대·휴일 여부 평균 기반 모델이었다. 이는 따릉이 대여량이 대여소 위치, 시간대, 휴일 여부에 크게 영향을 받는다는 점을 보여준다. 그러나 Baseline은 날씨, 최근 수요 흐름, 하루 전·일주일 전 반복 패턴을 동시에 반영하지 못하므로 머신러닝 모델을 통한 성능 개선 여지가 존재한다.

---

### 2-2. XGBoost

**하이퍼파라미터**

| 구분 | 기본 모델 | 튜닝 후보 1 | 튜닝 후보 2 | 튜닝 후보 3 |
|------|----------:|------------:|------------:|------------:|
| objective | count:poisson | count:poisson | count:poisson | count:poisson |
| eval_metric | rmse | rmse | rmse | rmse |
| learning_rate | 0.05 | 0.03 | 0.02 | 0.025 |
| max_depth | 8 | 10 | 9 | 9 |
| min_child_weight | 10 | 30 | 50 | 15 |
| subsample | 0.85 | 0.80 | 0.75 | 0.82 |
| colsample_bytree | 0.85 | 0.80 | 0.75 | 0.82 |
| reg_alpha | 0.1 | 0.3 | 0.5 | 0.2 |
| reg_lambda | 1.0 | 2.0 | 3.0 | 1.5 |
| gamma | - | 0.1 | 0.2 | 0.05 |
| n_estimators | 2000 | 3000 | 5000 | 4000 |

XGBoost는 count형 target인 대여량 예측에 맞춰 Poisson 목적 함수를 사용하였다. 대여량은 음수가 될 수 없는 값이므로, 일반 회귀보다 count 데이터의 특성을 반영하는 목적 함수가 적합하다고 판단하였다.

**성능 결과**

| 모델 | Dataset | MAE | RMSE | WAPE | R2 |
|------|---------|------:|------:|------:|------:|
| XGBoost 기본 모델 | Validation | 1.0380 | 1.7647 | 0.5684 | 0.7353 |
| XGBoost 기본 모델 | Test | 0.9503 | 1.6152 | 0.6013 | 0.7148 |
| XGBoost 튜닝 후보 1 | Validation | 1.0308 | 1.7525 | 0.5645 | 0.7389 |
| XGBoost 튜닝 후보 1 | Test | 0.9457 | 1.6086 | 0.5984 | 0.7171 |
| XGBoost 튜닝 후보 2 | Validation | 1.0350 | 1.7566 | 0.5668 | 0.7377 |
| XGBoost 튜닝 후보 2 | Test | 0.9480 | 1.6091 | 0.5998 | 0.7169 |
| XGBoost 튜닝 후보 3 | Validation | 1.0355 | 1.7579 | 0.5671 | 0.7373 |
| XGBoost 튜닝 후보 3 | Test | 0.9486 | 1.6103 | 0.6003 | 0.7165 |

**해석**

XGBoost 계열 모델은 Baseline 대비 모든 지표에서 큰 폭의 성능 개선을 보였다. 특히 튜닝 후보 1은 Test 기준 MAE 0.9457, RMSE 1.6086, WAPE 0.5984, R2 0.7171로 전체 후보 중 가장 우수하였다. 이는 대여소 정보, 시간 정보, 날씨 정보, Lag, Rolling, Diff Feature가 결합되었을 때 Tree 기반 모델이 따릉이 대여량의 비선형 패턴을 효과적으로 학습할 수 있음을 의미한다.

---

### 2-3. LightGBM

**하이퍼파라미터**

| 파라미터 | 값 |
|---------|-----|
| objective | poisson |
| metric | mae, rmse |
| boosting_type | gbdt |
| learning_rate | 0.05 |
| num_leaves | 96 |
| max_depth | -1 |
| min_data_in_leaf | 300 |
| feature_fraction | 0.85 |
| bagging_fraction | 0.85 |
| bagging_freq | 1 |
| lambda_l1 | 0.1 |
| lambda_l2 | 1.0 |
| num_boost_round | 3000 |
| early_stopping_rounds | 100 |

**성능 결과**

| Dataset | MAE | RMSE | WAPE | R2 |
|---------|------:|------:|------:|------:|
| Validation | 1.0358 | 1.7606 | 0.5672 | 0.7365 |
| Test | 0.9484 | 1.6125 | 0.6001 | 0.7157 |

**해석**

LightGBM은 XGBoost와 유사하게 우수한 성능을 보였다. 대용량 정형 데이터 처리에 강점이 있으며, 성능 또한 최종 후보군에 포함될 정도로 안정적이었다. 다만 Test 기준 MAE, RMSE, WAPE, R2에서 XGBoost 튜닝 후보 1보다 소폭 낮아 최종 모델로 선정하지는 않았다.

---

### 2-4. 하이퍼파라미터 튜닝

**튜닝 방법**

본 프로젝트에서는 XGBoost 기본 모델을 기준으로 learning rate, tree depth, min_child_weight, sampling 비율, L1/L2 정규화, gamma 값을 조정하여 튜닝 후보를 구성하고 성능을 비교하였다.

| 파라미터 | 탐색 방향 | 튜닝 목적 |
|---------|----------|----------|
| learning_rate | 0.05보다 낮은 값으로 조정 | 학습 속도를 낮춰 일반화 성능 개선 |
| n_estimators | 반복 횟수 증가 | 낮은 learning rate 보완 |
| max_depth | 8~10 범위 조정 | 복잡한 패턴 학습과 과적합 제어 균형 |
| min_child_weight | 증가 방향 조정 | 작은 샘플 분기에 대한 과도한 학습 방지 |
| subsample | 0.75~0.85 범위 조정 | 데이터 sampling을 통한 과적합 완화 |
| colsample_bytree | 0.75~0.85 범위 조정 | Feature sampling을 통한 과적합 완화 |
| reg_alpha | 증가 방향 조정 | L1 정규화로 불필요한 분기 억제 |
| reg_lambda | 증가 방향 조정 | L2 정규화로 모델 안정성 확보 |
| gamma | 추가 적용 | 분기 조건을 강화하여 불필요한 split 감소 |

**튜닝 전/후 비교, Test 기준**

| 지표 | 튜닝 전 | 튜닝 후 | 개선 |
|------|--------:|--------:|------:|
| MAE | 0.9503 | 0.9457 | 0.0046 감소 |
| RMSE | 1.6152 | 1.6086 | 0.0066 감소 |
| WAPE | 0.6013 | 0.5984 | 0.0029 감소 |
| R2 | 0.7148 | 0.7171 | 0.0023 증가 |

**해석**

튜닝을 통해 모든 핵심 지표가 개선되었다. 개선 폭은 크지 않지만, 이미 Baseline 대비 크게 개선된 Tree 기반 모델에서 추가로 성능을 끌어올렸다는 점에서 의미가 있다. 특히 MAE와 WAPE가 함께 감소했기 때문에 전체적인 평균 오차와 전체 수요 대비 오차가 모두 줄어든 것으로 해석할 수 있다.

---

## 3. 딥러닝 모델 학습 결과 (PyTorch)

### 3-1. 모델 구조

**네트워크 아키텍처**

```text
입력 Sequence: 대여소별 직전 24시간 Feature
    │
    ├─ Station Embedding
    ├─ GRU Layer(hidden_size=64, num_layers=1)
    ├─ Dropout(0.1)
    └─ Output Layer → rental_count 예측
```

| 항목 | 값 |
|------|-----|
| 모델 유형 | GRU 기반 Sequence 회귀 모델 |
| 입력 길이 | 24시간 |
| hidden_size | 64 |
| num_layers | 1 |
| dropout | 0.1 |
| station_emb_dim | 16 |
| 출력값 | 예측 대여량 |

GRU 모델은 각 대여소의 직전 24시간 흐름을 입력으로 받아 현재 시점의 대여량을 예측하도록 설계하였다. 대여소별 고유 패턴을 반영하기 위해 station embedding을 함께 사용하였다.

---

### 3-2. 학습 설정 (Hyperparameters)

| 파라미터 | 값 |
|---------|-----|
| 손실 함수 | 회귀 손실 함수 기반 학습 |
| 옵티마이저 | Adam 계열 |
| 학습률 | 0.001 |
| 배치 크기 | 1024 |
| Epochs | 10 |
| Early Stopping | Patience 3 |
| weight_decay | 0.00001 |
| 학습 장치 | CPU |

---

### 3-3. 학습 과정

GRU는 대여소별 시간 순서를 유지한 상태에서 직전 24시간 sequence를 구성하여 학습하였다. 각 대여소의 첫 24개 행은 충분한 과거 sequence가 없기 때문에 평가에서 제외하였다.

| 학습 조건 | 내용 |
|----------|------|
| Sequence 구성 | station_id별 시간 순서 기준 직전 24시간 |
| 예측 대상 | 현재 시점 rental_count |
| 제외 데이터 | 각 대여소별 초기 24시간 구간 |
| Early Stopping | 검증 성능 개선 여부 기준으로 적용 |

**해석**

GRU는 시간 흐름을 직접 학습할 수 있다는 장점이 있었지만, 본 프로젝트에서는 Lag, Rolling, Diff 변수를 통해 시계열 정보가 이미 Feature로 충분히 반영되어 있었다. 따라서 Tree 기반 모델 대비 성능 우위를 보이지 못하였다.

---

### 3-4. 딥러닝 성능 결과

| Dataset | MAE | RMSE | WAPE | R2 |
|---------|------:|------:|------:|------:|
| Validation | 1.0422 | 1.8242 | 0.5698 | 0.7177 |
| Test | 0.9678 | 1.7021 | 0.6114 | 0.6839 |

GRU는 Baseline보다는 개선된 성능을 보였지만, XGBoost와 LightGBM보다 낮은 성능을 보였다. 특히 RMSE와 R2에서 차이가 나타났으며, 이는 피크 시간대의 수요 변동을 Tree 기반 모델만큼 안정적으로 잡지 못했을 가능성을 의미한다.

---

## 4. 모델 종합 비교 및 최적 모델 선정

### 4-1. 전체 모델 성능 비교표 (Test 기준)

| 모델 | MAE | RMSE | WAPE | R2 |
|------|------:|------:|------:|------:|
| Baseline 최우수 모델 | 1.2635 | 2.1827 | 0.7995 | 0.4791 |
| XGBoost 기본 모델 | 0.9503 | 1.6152 | 0.6013 | 0.7148 |
| XGBoost 튜닝 후보 1 | 0.9457 | 1.6086 | 0.5984 | 0.7171 |
| XGBoost 튜닝 후보 2 | 0.9480 | 1.6091 | 0.5998 | 0.7169 |
| XGBoost 튜닝 후보 3 | 0.9486 | 1.6103 | 0.6003 | 0.7165 |
| LightGBM | 0.9484 | 1.6125 | 0.6001 | 0.7157 |
| GRU | 0.9678 | 1.7021 | 0.6114 | 0.6839 |

---

### 4-2. 지표별 최고 성능 모델

| 지표 | 최고 모델 | 값 |
|------|----------|-----:|
| MAE | XGBoost 튜닝 후보 1 | 0.9457 |
| RMSE | XGBoost 튜닝 후보 1 | 1.6086 |
| WAPE | XGBoost 튜닝 후보 1 | 0.5984 |
| R2 | XGBoost 튜닝 후보 1 | 0.7171 |

모든 핵심 지표에서 XGBoost 튜닝 후보 1이 가장 우수하였다. 따라서 단일 지표에 의존한 선택이 아니라, 평균 오차, 큰 오차, 전체 수요 대비 오차, 설명력 측면에서 모두 일관되게 우수한 모델을 최종 모델로 선정하였다.

---

### 4-3. 성능 비교 해석

본 프로젝트는 회귀 예측 문제이므로 ROC Curve나 혼동 행렬 대신, Test 데이터 기준 회귀 성능 지표를 중심으로 모델을 비교하였다.

Baseline 최우수 모델 대비 최종 모델의 개선 효과는 다음과 같다.

| 지표 | Baseline 최우수 모델 | 최종 모델 | 개선 효과 |
|------|--------------------:|----------:|----------:|
| MAE | 1.2635 | 0.9457 | 약 25.2% 감소 |
| RMSE | 2.1827 | 1.6086 | 약 26.3% 감소 |
| WAPE | 0.7995 | 0.5984 | 약 25.2% 감소 |
| R2 | 0.4791 | 0.7171 | 0.2380 증가 |

이 결과는 단순히 대여소·시간대 평균을 사용하는 것보다, 날씨 정보와 과거 수요 흐름을 함께 반영한 머신러닝 모델이 실제 대여량 변동을 더 잘 설명한다는 것을 의미한다.

---

### 4-4. 최적 모델 선정

**선정 모델**: XGBoost 튜닝 모델

**선정 근거**

1. Test 기준 MAE, RMSE, WAPE, R2에서 모두 최고 성능을 기록하였다.
2. Baseline 최우수 모델 대비 MAE 약 25.2%, RMSE 약 26.3%, WAPE 약 25.2% 개선을 보였다.
3. Validation과 Test 성능이 모두 안정적이며, 특정 데이터 구간에만 과도하게 맞춰진 결과로 보기 어렵다.
4. Lag, Rolling, Diff, 시간 정보, 날씨 정보 등 최종 Feature를 효과적으로 활용하였다.
5. pkl 파일로 저장하여 백엔드 API에서 바로 불러와 예측에 활용할 수 있다.

**트레이드오프 분석**

LightGBM은 학습 속도와 대용량 처리 측면에서 장점이 있으며 성능도 우수하였다. 그러나 Test 기준 핵심 지표에서 최종 XGBoost 튜닝 모델보다 소폭 낮았다. GRU는 시간 흐름을 직접 학습할 수 있다는 장점이 있었지만, 현재 Feature Engineering이 충분히 반영된 데이터에서는 Tree 기반 모델보다 성능이 낮았다. 따라서 실무 적용성과 성능을 함께 고려했을 때 XGBoost 튜닝 모델을 최종 모델로 선정하는 것이 가장 타당하다.

---

### 4-5. 예측값 활용 기준

본 프로젝트는 분류 문제가 아니므로 별도의 classification threshold는 적용하지 않는다. 모델 출력값은 특정 대여소·시간대의 예상 대여량이다.

다만 프론트엔드 및 운영 화면에서는 예측 대여량을 사용자 친화적으로 보여주기 위해 다음과 같은 후처리 기준을 적용할 수 있다.

| 예측 대여량 | 수요 수준 | 활용 예시 |
|-------------|----------|----------|
| 1대 미만 | 매우 낮음 | 일반 모니터링 |
| 1대 이상 3대 미만 | 낮음 | 수요 낮은 시간대 표시 |
| 3대 이상 7대 미만 | 보통 | 일반 운영 상태 |
| 7대 이상 12대 미만 | 높음 | 자전거 부족 가능성 확인 |
| 12대 이상 | 매우 높음 | 사전 재배치 검토 |

이 기준은 모델 학습 기준이 아니라 서비스 화면에서 결과를 해석하기 위한 운영용 후처리 기준이다.

---

## 5. 모델 해석

### 5-1. 특성 중요도 (Feature Importance)

최종 XGBoost 튜닝 모델의 주요 Feature Importance는 다음과 같다.

| 순위 | 특성명 | 중요도 | 해석 |
|------|--------|------:|------|
| 1 | lag_1 | 0.5212 | 직전 1시간 대여량이 현재 수요 예측에 가장 큰 영향을 미친다. |
| 2 | lag_168 | 0.1458 | 일주일 전 같은 시간대 패턴이 반복 수요를 설명한다. |
| 3 | rolling_mean_3 | 0.1089 | 최근 3시간 평균 흐름이 단기 수요 추세를 반영한다. |
| 4 | lag_24 | 0.0765 | 하루 전 같은 시간대 수요가 현재 시간대 예측에 중요하다. |
| 5 | precipitation | 0.0254 | 강수량은 자전거 이용 감소 가능성을 설명하는 핵심 날씨 변수이다. |
| 6 | is_day_off | 0.0142 | 평일과 휴일의 수요 차이를 반영한다. |
| 7 | rolling_mean_24 | 0.0142 | 최근 하루 평균 수요 흐름을 반영한다. |
| 8 | hour_cos | 0.0130 | 시간대의 순환 구조를 보완한다. |
| 9 | lag_48 | 0.0126 | 이틀 전 같은 시간대 수요 패턴을 반영한다. |
| 10 | hour | 0.0102 | 출퇴근 시간대와 심야 시간대의 수요 차이를 반영한다. |

**해석**

상위 Feature는 대부분 과거 대여량과 시간 패턴 관련 변수이다. 이는 따릉이 수요가 단순히 날씨만으로 결정되는 것이 아니라, 직전 수요 흐름과 하루 전·일주일 전 반복 패턴에 강하게 의존한다는 것을 보여준다. 또한 강수량과 휴일 여부도 상위권에 포함되어, 날씨와 생활 패턴이 실제 수요 예측에 의미 있게 반영되었음을 확인할 수 있다.

---

### 5-2. SHAP 분석

본 결과서에서는 SHAP 분석을 별도로 수행하지 않았다. 대신 최종 모델의 Feature Importance를 통해 주요 변수의 상대적 영향도를 해석하였다. 향후 모델 해석력을 더 강화하려면 SHAP Summary Plot을 추가하여 각 변수가 예측값을 높이는 방향인지 낮추는 방향인지까지 분석할 수 있다.

---

### 5-3. 수요 발생 프로파일

최종 모델의 Feature Importance와 도메인 특성을 바탕으로, 대여량이 증가할 가능성이 높은 조건은 다음과 같이 해석할 수 있다.

- 직전 1시간 대여량이 높은 대여소
- 최근 3시간 평균 대여량이 상승 중인 대여소
- 하루 전 또는 일주일 전 같은 시간대 수요가 높았던 대여소
- 출퇴근 시간대에 해당하는 시간대
- 비가 오지 않거나 강수량이 낮은 시간대
- 평일·휴일 패턴이 뚜렷하게 나타나는 대여소

운영 관점에서는 위 조건에 해당하는 대여소를 중심으로 자전거 부족 가능성을 사전에 확인하고, 재배치 또는 모니터링 우선순위를 설정할 수 있다.

---

## 6. 최종 결과 요약

본 프로젝트에서는 Baseline, XGBoost, LightGBM, GRU를 비교하여 따릉이 대여소별·시간대별 대여량 예측 모델을 학습하였다. 최종 비교 결과 XGBoost 튜닝 모델이 Test 기준 MAE 0.9457, RMSE 1.6086, WAPE 0.5984, R2 0.7171로 가장 우수하였다.

최종 모델은 Baseline 최우수 모델 대비 MAE 약 25.2%, RMSE 약 26.3%, WAPE 약 25.2%를 개선하였다. 또한 특성 중요도 분석 결과 직전 1시간 대여량, 일주일 전 같은 시간대 대여량, 최근 3시간 평균 대여량, 하루 전 같은 시간대 대여량이 주요 변수로 나타났다.

따라서 본 프로젝트에서는 학습 성능, 해석 가능성, 서비스 적용 가능성을 종합적으로 고려하여 XGBoost 튜닝 모델을 최종 모델로 선정하였다. 학습된 모델은 pkl 파일로 저장되어 백엔드 API에서 예측 엔진으로 활용할 수 있으며, 프론트엔드에서는 대여소와 날짜·시간을 선택하면 예상 대여량과 수요 수준을 확인하는 방식으로 서비스화할 수 있다.
