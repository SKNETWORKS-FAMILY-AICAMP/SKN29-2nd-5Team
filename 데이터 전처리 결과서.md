# 데이터 전처리 결과서

## 1. 프로젝트 개요

### 1-1. 비즈니스 배경
서울시 공공자전거 따릉이는 시간대, 요일, 계절, 날씨, 대여소 위치 등에 따라 대여 수요가 크게 달라진다. 특정 시간대나 특정 지역에 수요가 집중되면 자전거 부족 또는 과잉 배치 문제가 발생할 수 있다.

### 1-2. 비즈니스 목표
대여소별·시간대별 예상 대여량을 예측하여 자전거 재배치와 운영 의사결정을 지원한다.

### 1-3. 머신러닝 활용 계획
| 항목 | 내용 |
|------|------|
| 문제 유형 | 회귀 |
| 예측 대상 | 대여소별 시간 단위 대여량 |
| 활용 방안 | 특정 대여소, 날짜, 시간 정보를 입력하면 예상 대여량을 예측하여 운영 의사결정에 활용|

### 1-4. 성공 기준

본 프로젝트는 대여량을 예측하는 **회귀 문제**이므로 분류 문제에서 사용하는 Accuracy, Precision, Recall, F1-score는 주요 평가 지표로 사용하지 않는다.  
대신 실제 대여량과 예측 대여량의 차이를 확인할 수 있는 회귀 평가 지표를 사용한다.

| 평가 지표 | 사용 목적 |
|---|---|
| MAE | 평균적으로 몇 대 정도 예측이 틀리는지 확인 |
| RMSE | 큰 오차가 발생하는 구간을 더 민감하게 확인 |
| WAPE | 전체 대여량 대비 오차 비율 확인 |
| R² | 모델이 대여량 변동을 어느 정도 설명하는지 확인 |

본 프로젝트에서는 **MAE와 WAPE를 중심 지표**로 사용하고, RMSE와 R²는 보조 지표로 활용한다.  
MAE는 운영 관점에서 "평균적으로 몇 대 정도 차이가 나는지" 해석하기 쉽고, WAPE는 전체 대여량 규모 대비 오차 수준을 확인할 수 있기 때문이다.

---

## 2. 데이터셋 소개

### 2-1. 데이터 기본 정보

| 항목 | 내용 |
|------|------|
| 데이터셋명 | 서울시 공공자전거 따릉이 대여량 예측 데이터셋 |
| 출처 | 서울시 공공데이터 기반 따릉이 대여 이력 및 관련 데이터 |
| 수집 기간 | 2023년 ~ 2025년 |
| 전체 샘플 수 | 70,256,119건 |
| 특성(Feature) 수 | 원본 대여 이력 데이터 기준 17개 컬럼 |
| 타겟(Target) | 시간 단위 대여량 `rental_count` |
| 데이터 불균형 여부 | 회귀 문제이므로 클래스 불균형 개념은 적용하지 않음. 단, 대여량 0인 시간대가 많이 포함됨 |
※ 전체 샘플 수는 전처리 후 생성된 Train/Validation/Test 데이터 기준이다.

### 2-2. 특성 목록 및 설명

본 프로젝트에서 사용한 원본 데이터는 따릉이 대여 이력 데이터, 대여소 정보 데이터, 날씨 데이터, 공휴일 데이터로 구성된다.  
이 중 핵심이 되는 원본 대여 이력 데이터는 개별 자전거 대여 및 반납 기록 단위로 구성되어 있으며, 주요 컬럼은 다음과 같다.

| 특성명 | 데이터 타입 | 설명 | 예시값 |
|--------|------------|------|--------|
| 자전거번호 | object | 대여된 자전거의 고유 번호 | SPB-00000 |
| 대여일시 | datetime | 자전거가 대여된 날짜와 시간 | 2024-01-01 00:03:00 |
| 대여 대여소번호 | int64 | 대여가 발생한 대여소 번호 | 102 |
| 대여 대여소명 | object | 대여가 발생한 대여소 이름 | 망원역 1번출구 앞 |
| 대여거치대 | object 또는 int64 | 대여 시 사용된 거치대 정보 | 1 |
| 반납일시 | datetime | 자전거가 반납된 날짜와 시간 | 2024-01-01 00:18:00 |
| 반납대여소번호 | int64 | 반납이 발생한 대여소 번호 | 102 |
| 반납대여소명 | object | 반납이 발생한 대여소 이름 | 망원역 1번출구 앞 |
| 반납거치대 | object 또는 int64 | 반납 시 사용된 거치대 정보 | 1 |
| 이용시간(분) | int64 | 자전거 이용 시간 | 15 |
| 이용거리(M) | int64 또는 float64 | 자전거 이용 거리 | 1200 |
| 생년 | object 또는 int64 | 이용자 출생연도 정보 | 1995 |
| 성별 | object | 이용자 성별 | M / F |
| 이용자종류 | object | 이용권 또는 이용자 유형 | 정기권 |
| 대여대여소ID | object | 대여 대여소의 ID | ST-102 |
| 반납대여소ID | object | 반납 대여소의 ID | ST-102 |
| 자전거구분 | object | 자전거 유형 구분 | 일반자전거 |

### 2-3. 타겟 변수 분포

본 프로젝트의 타겟 변수는 `rental_count`이며, 특정 대여소에서 특정 날짜와 시간에 발생한 대여 건수를 의미한다.

원본 대여 이력 데이터는 개별 대여 기록 단위로 구성되어 있으므로, 모델 학습을 위해 `station_id`, 날짜, 시간 단위로 대여 건수를 집계하여 `rental_count`를 생성하였다.

따릉이 대여량 데이터는 모든 대여소에서 매 시간 대여가 발생하지 않기 때문에 `rental_count = 0`인 데이터가 많이 포함되어 있다.  
또한 일부 대여소와 특정 시간대에는 대여량이 집중되어, 전체적으로 작은 값이 많고 일부 큰 값이 존재하는 비대칭 분포를 보인다.

| 항목 | 내용 |
|------|------|
| 타겟 변수 | `rental_count` |
| 생성 기준 | `station_id` × 날짜 × 시간 단위 대여 건수 집계 |
| 최소값 | 0 |
| 평균 | 약 1.766 |
| 0 대여량 비율 | 약 0.464 |
| 분포 특성 | 0 값이 많고 일부 큰 대여량이 존재하는 비대칭 분포 |

이러한 타겟 변수의 특성을 고려하여 모델 평가 시 평균적인 예측 오차를 확인할 수 있는 MAE와 전체 대여량 대비 오차 비율을 확인할 수 있는 WAPE를 주요 지표로 사용하였다.

### 2-4. 데이터 특성

따릉이 대여량 데이터는 일반적인 회귀 데이터와 달리 다음과 같은 특성을 가진다.

| 특성 | 설명 |
|---|---|
| 시간 의존성 | 과거 대여량이 미래 대여량에 영향을 줄 수 있음 |
| 계절성 | 월, 계절, 날씨에 따라 수요 차이 발생 |
| 주기성 | 하루 단위, 일주일 단위 반복 패턴 존재 |
| 지역성 | 대여소 위치에 따라 평균 수요가 다름 |
| 0 대여량 다수 | 모든 대여소에서 매시간 대여가 발생하지 않음 |

---

## 3. 탐색적 데이터 분석 (EDA)

본 프로젝트의 원본 따릉이 대여 이력 데이터는 연도별·월별 파일 단위로 구성되어 있으며, 전체 데이터 크기가 커서 모든 원본 데이터를 한 번에 메모리에 적재하여 상세 EDA를 수행하기 어려웠다.

따라서 일부 월별 샘플 데이터를 통해 원본 데이터 구조를 먼저 확인하고, 이후 전처리 과정에서 연도별·월별 데이터를 순차적으로 처리하였다.  
또한 본 프로젝트는 반납량이 아니라 대여량 예측이 목적이므로, `반납일시`, `반납대여소ID`보다 `대여일시`, `대여대여소ID`를 중심으로 데이터를 확인하였다.

### 3-1. 기초 통계량

원본 데이터의 구조를 확인하기 위해 `df.info()`와 `df.describe()`를 활용하여 컬럼 수, 데이터 타입, 주요 수치형 변수의 기초 통계량을 확인하였다.

| 항목 | 확인 내용 |
|------|------|
| 원본 컬럼 수 | 17개 |
| 기준 시간 컬럼 | `대여일시` |
| 기준 대여소 컬럼 | `대여대여소ID` |
| 주요 수치형 변수 | `이용시간(분)`, `이용거리(M)` |
| 주요 범주형 변수 | `자전거번호`, `대여 대여소명`, `성별`, `이용자종류`, `자전거구분` |

원본 데이터는 개별 대여 및 반납 기록 단위로 구성되어 있었기 때문에, 모델 학습을 위해 대여소·날짜·시간 단위로 재집계가 필요하다고 판단하였다.

### 3-2. 결측값 분석

원본 대여 이력 데이터와 외부 결합 데이터에서 모델 학습에 필요한 주요 값의 누락 여부를 확인하였다.  
본 프로젝트에서는 대여량 예측이 목적이므로 `대여일시`, `대여대여소ID`, 대여소 정보, 날씨 정보, 공휴일 여부를 중심으로 확인하였다.

| 특성명 | 확인 내용 | 처리 방안 |
|--------|-----------|----------|
| `대여일시` | 대여량을 날짜·시간 단위로 집계하기 위한 기준 컬럼 | 대여일시 기준으로 날짜와 시간을 추출 |
| `대여대여소ID` | 대여소별 대여량을 집계하기 위한 기준 컬럼 | 대여소 ID 기준으로 시간대별 대여량 집계 |
| 대여소 정보 | 대여 이력의 대여소 ID와 대여소 정보 파일을 비교하여 매칭 여부 확인 | 매칭 가능한 대여소 정보는 보완하고, 끝까지 매칭되지 않는 대여소는 제거 |
| 대여소 위치 정보 | 위도, 경도 등 위치 기반 정보 결합 여부 확인 | 대여소 마스터 정보와 결합하여 사용 |
| 날씨 정보 | 날짜·시간 기준으로 대여량 데이터와 결합 가능 여부 확인 | 날짜·시간 기준으로 병합하여 기온, 강수량 등 외부 변수로 활용 |
| 공휴일 정보 | 원본 결측값을 처리한 것이 아니라 날짜 기준으로 주말 또는 공휴일 여부 생성 | 날짜 기준으로 `is_day_off` 파생 변수 생성 |

대여소 정보 결합 과정에서 일부 대여소 ID는 대여소 마스터 데이터와 매칭되지 않았다.  
해당 대여소는 위치 정보와 대여소 속성 정보를 신뢰하기 어렵기 때문에 최종 학습 데이터에서 제거하였다.
최종적으로 제거한 미매칭 대여소는 58개이며, 이는 대여소 정보 결합 후 전체 대여 이력 기준 약 0.896% 수준이었다.

### 3-3. 이상값 분석

이상값은 원본 수치형 변수와 집계 후 생성된 대여량을 중심으로 확인하였다.

| 분석 대상 | 확인 내용 | 처리 방법 |
|-----------|-----------|-----------|
| `이용시간(분)` | 지나치게 짧거나 긴 이용시간 존재 여부 확인 | 최종 타겟 생성에는 직접 사용하지 않고 참고용으로 확인 |
| `이용거리(M)` | 0 또는 비정상적으로 큰 거리 존재 여부 확인 | 최종 타겟 생성에는 직접 사용하지 않고 참고용으로 확인 |
| `rental_count` | 특정 대여소·시간대의 대여량 급증 여부 확인 | 실제 수요 집중일 수 있으므로 일괄 제거하지 않음 |
| 날씨 변수 | 기온, 강수량, 풍속 등의 비정상 값 확인 | 날짜·시간 기준으로 대여량 데이터와 결합 가능한 값만 활용 |

대여량이 매우 큰 값은 출퇴근 시간대, 특정 지역 수요 집중, 행사 등 실제 현상일 수 있으므로 단순 이상값으로 판단하여 일괄 제거하지 않았다.

### 3-4. 특성별 분포 분석

따릉이 대여량은 시간, 요일, 월, 대여소, 날씨 조건에 따라 달라질 수 있으므로 주요 특성별 분포를 확인하였다.

| 분석 항목 | 확인 목적 |
|-----------|-----------|
| 시간대별 대여량 | 특정 시간대에 수요가 집중되는지 확인 |
| 요일별 대여량 | 평일과 주말의 수요 차이 확인 |
| 월별 대여량 | 계절성 및 월별 수요 차이 확인 |
| 대여소별 대여량 | 대여소별 평균 수요 차이 확인 |
| 날씨별 대여량 | 기온, 강수량, 풍속 등에 따른 수요 변화 확인 |
| 0 대여량 비율 | 대여가 발생하지 않는 시간대 비중 확인 |

분석 결과를 바탕으로 시간 정보, 대여소 정보, 날씨 정보, 공휴일 정보를 전처리 및 피처 엔지니어링에 반영하였다.

### 3-5. 상관관계 분석

수치형 변수 간의 관계를 확인하기 위해 상관관계 분석을 수행하였다.  
특히 날씨 변수, 시간 변수, 과거 대여량 기반 변수와 타겟 변수 `rental_count`의 관계를 확인하였다.

| 분석 대상 | 확인 목적 |
|-----------|-----------|
| 날씨 변수와 `rental_count` | 기온, 강수량, 풍속 등이 대여량에 미치는 영향 확인 |
| 시간 변수와 `rental_count` | 시간, 요일, 월에 따른 대여량 차이 확인 |
| 과거 대여량과 `rental_count` | lag 피처의 예측 가능성 확인 |
| 수치형 피처 간 상관관계 | 중복 정보 또는 다중공선성 가능성 확인 |

본 프로젝트에서는 LightGBM, XGBoost와 같은 트리 기반 모델을 주요 후보로 고려하였기 때문에 선형 모델만큼 다중공선성에 민감하지는 않지만, 중복 정보가 많은 변수는 모델 복잡도를 높일 수 있으므로 참고 지표로 활용하였다.

### 3-6. EDA 주요 발견사항 (Insight)

EDA를 통해 확인한 주요 내용은 다음과 같다.

| 발견사항 | 이후 전처리 반영 |
|----------|----------------|
| 원본 데이터는 개별 대여 기록 단위로 구성되어 있음 | 대여소 × 날짜 × 시간 단위로 집계 |
| 프로젝트 목적은 반납량이 아니라 대여량 예측임 | `대여일시`, `대여대여소ID` 중심으로 데이터 구성 |
| 대여량은 시간대별 차이가 존재할 수 있음 | `hour`, 출퇴근 시간대 피처 생성 |
| 요일 및 쉬는 날 여부에 따라 수요 차이가 발생할 수 있음 | `dayofweek`, `is_day_off` 생성 |
| 월별·계절별 수요 차이가 존재할 수 있음 | `month`, 날씨 정보 반영 |
| 대여소별 평균 수요 차이가 존재할 수 있음 | 대여소 ID 및 대여소 정보 결합 |
| 대여가 없는 시간대가 많이 존재함 | 0 대여량을 제거하지 않고 학습 데이터에 포함 |
| 일부 대여소 ID는 마스터 데이터와 매칭되지 않음 | 미매칭 대여소 제거 |
| 과거 대여량이 현재 대여량 예측에 중요할 수 있음 | lag 및 rolling 피처 생성 |

이러한 EDA 결과를 바탕으로 시간 정보, 대여소 정보, 날씨 정보, 공휴일 정보, 과거 대여량 기반 피처를 포함한 학습 데이터셋을 구성하였다.
---

## 4. 데이터 전처리

### 4-1. 결측값 처리

원본 대여 이력 데이터와 외부 데이터 결합 과정에서 모델 학습에 필요한 주요 값의 누락 여부를 확인하였다.  
본 프로젝트는 대여량 예측이 목적이므로 `대여일시`, `대여대여소ID`, 대여소 정보, 날씨 정보, 공휴일 정보를 중심으로 데이터를 정리하였다.

| 구분 | 처리 방법 | 근거 |
|------|----------|------|
| `대여일시` | 대여일시 기준으로 날짜와 시간을 추출 | 대여량을 날짜·시간 단위로 집계하기 위한 필수 기준 컬럼 |
| `대여대여소ID` | 대여소 ID 기준으로 시간대별 대여량 집계 | 대여소별 수요 예측을 위한 필수 기준 컬럼 |
| 대여소 정보 미매칭 | 대여소 정보 파일과 비교하여 가능한 값은 보완하고, 끝까지 매칭되지 않는 대여소는 제거 | 위치 정보 및 대여소 속성 정보의 신뢰성 확보 |
| 날씨 정보 | 날짜·시간 기준으로 대여량 데이터와 결합 가능한 값 활용 | 기온, 습도, 풍속, 강수량 등 외부 환경 요인 반영 |
| 공휴일 정보 | 날짜 기준으로 주말 또는 공휴일 여부를 생성 | 쉬는 날 여부에 따른 수요 차이 반영 |

대여소 정보 결합 과정에서 일부 대여소 ID는 대여소 마스터 데이터와 매칭되지 않았다.  
해당 대여소는 위치 정보와 대여소 속성 정보를 신뢰하기 어렵기 때문에 최종 학습 데이터에서 제거하였다.

최종적으로 제거한 미매칭 대여소는 58개이며, 이는 대여소 정보 결합 후 전체 대여 이력 기준 약 0.896% 수준이었다.

---

### 4-2. 이상값 처리

이상값은 원본 수치형 변수와 집계 후 생성된 대여량을 중심으로 확인하였다.

| 구분 | 처리 방법 | 근거 |
|------|----------|------|
| `이용시간(분)` | 최종 모델 입력에서는 직접 사용하지 않고 참고용으로 확인 | 본 프로젝트의 타겟은 이용시간이 아니라 대여량이기 때문 |
| `이용거리(M)` | 최종 모델 입력에서는 직접 사용하지 않고 참고용으로 확인 | 대여량 예측 목적과 직접적인 관련성이 낮고, 예측 시점에 알 수 없는 정보이기 때문 |
| `rental_count` | 큰 값을 일괄 제거하지 않음 | 출퇴근 시간대, 특정 지역 수요 집중 등 실제 수요일 가능성이 있음 |
| 날씨 변수 | 날짜·시간 기준으로 대여량 데이터와 결합 가능한 값 활용 | 기상 조건에 따른 수요 변화를 반영하기 위함 |

대여량이 매우 큰 값은 단순 오류라기보다 특정 시간대나 특정 대여소의 실제 수요 집중일 가능성이 있다.  
따라서 대여량 기준 이상값을 일괄 제거하지 않고 모델이 학습할 수 있도록 유지하였다.

---

### 4-3. 데이터 형식 변환 및 기준 컬럼 정리

원본 데이터는 개별 대여 및 반납 기록 단위로 구성되어 있었다.  
본 프로젝트는 반납량이 아니라 대여량 예측이 목적이므로, `대여일시`와 `대여대여소ID`를 기준으로 데이터를 정리하였다.

| 처리 항목 | 처리 내용 |
|----------|----------|
| 기준 시간 컬럼 | `대여일시` 사용 |
| 기준 대여소 컬럼 | `대여대여소ID` 사용 |
| 날짜/시간 변환 | `대여일시`를 datetime 형식으로 변환 |
| 시간 단위 변환 | 날짜와 시간을 추출하여 시간 단위 집계 기준 생성 |
| 반납 관련 컬럼 | 최종 대여량 예측 기준에서는 제외 |

`대여일시`에서 연도, 월, 일, 시간, 요일 정보를 추출하여 시간 기반 피처 생성에 활용하였다.

---

### 4-4. 특성 공학 (Feature Engineering)

대여량 예측 성능을 높이기 위해 시간 정보, 대여소 정보, 날씨 정보, 쉬는 날 여부, 과거 대여량 정보를 활용한 파생 변수를 생성하였다.

| 생성 피처 | 설명 | 생성 이유 |
|----------|------|----------|
| `station_id` | 대여소 ID | 대여소별 수요 차이 반영 |
| `latitude` | 대여소 위도 | 대여소 위치 정보 반영 |
| `longitude` | 대여소 경도 | 대여소 위치 정보 반영 |
| `rack_count` | 거치대 수 | 대여소 규모 정보 반영 |
| `station_age_days` | 대여소 운영 기간 | 대여소 신설/운영 기간 차이 반영 |
| `month` | 월 | 월별·계절별 수요 차이 반영 |
| `day` | 일 | 날짜 정보 반영 |
| `hour` | 시간 | 시간대별 수요 패턴 반영 |
| `dayofweek` | 요일 | 요일별 수요 차이 반영 |
| `is_day_off` | 주말 또는 공휴일 여부 | 평일과 쉬는 날의 수요 차이 반영 |
| `hour_sin` | 시간의 주기성을 반영한 sin 변환 값 | 23시와 0시처럼 이어지는 시간 구조 반영 |
| `hour_cos` | 시간의 주기성을 반영한 cos 변환 값 | 시간대의 순환 패턴 반영 |
| `temperature` | 기온 | 기온에 따른 따릉이 이용 변화 반영 |
| `humidity` | 습도 | 습도에 따른 이용 변화 반영 |
| `wind_speed` | 풍속 | 바람이 강한 날의 이용 변화 반영 |
| `precipitation` | 강수량 | 비 오는 날의 이용 감소 가능성 반영 |
| `lag_1` | 1시간 전 대여량 | 직전 시간의 수요 흐름 반영 |
| `lag_2` | 2시간 전 대여량 | 단기 수요 흐름 반영 |
| `lag_3` | 3시간 전 대여량 | 단기 수요 흐름 반영 |
| `lag_24` | 24시간 전 대여량 | 전일 같은 시간대 수요 패턴 반영 |
| `lag_48` | 48시간 전 대여량 | 이틀 전 같은 시간대 수요 패턴 반영 |
| `lag_168` | 1주 전 같은 시간 대여량 | 주 단위 반복 패턴 반영 |
| `rolling_mean_3` | 최근 3시간 평균 대여량 | 단기 평균 수요 흐름 반영 |
| `rolling_mean_6` | 최근 6시간 평균 대여량 | 반나절 이내의 수요 흐름 반영 |
| `rolling_mean_24` | 최근 24시간 평균 대여량 | 일 단위 평균 수요 흐름 반영 |
| `rolling_std_24` | 최근 24시간 대여량 표준편차 | 하루 동안의 수요 변동성 반영 |
| `diff_1` | 1시간 전 대비 변화량 | 직전 시간 대비 수요 변화 반영 |
| `diff_24` | 전일 같은 시간 대비 변화량 | 전일 대비 수요 변화 반영 |

초기에는 주말 여부와 공휴일 여부를 별도로 둘 수도 있었지만, 최종 학습 데이터에서는 이를 분리하지 않고 `is_day_off` 하나로 통합하였다.  
즉, `is_day_off`는 주말 또는 공휴일에 해당하면 1, 일반 평일이면 0으로 처리한 변수이다.

lag, rolling, diff 계열 피처는 반드시 시간 순서를 기준으로 생성하였다.  
예측 시점 이후의 정보를 사용하지 않도록 하여 데이터 누수를 방지하였다.

---

### 4-5. 대여소 정보 및 외부 데이터 결합

대여량은 대여소 위치, 대여소 규모, 날씨, 쉬는 날 여부에 따라 달라질 수 있으므로 외부 데이터를 결합하였다.

| 데이터 | 결합 기준 | 활용 목적 |
|--------|----------|----------|
| 대여소 마스터 데이터 | 대여소 ID | 위도, 경도, 거치대 수, 대여소 운영 기간 등 대여소 속성 정보 반영 |
| 날씨 데이터 | 날짜·시간 | 기온, 습도, 풍속, 강수량 등 기상 조건 반영 |
| 공휴일 데이터 | 날짜 | 주말 또는 공휴일 여부를 `is_day_off`로 반영 |

대여소 정보는 대여 이력 데이터의 대여소 ID와 대여소 마스터 데이터를 비교하여 결합하였다.  
대여소 정보가 여러 파일에 나뉘어 있는 경우 가능한 정보를 우선적으로 보완한 뒤, 최종적으로 매칭되지 않는 대여소는 제거하였다.

공휴일 정보는 원본 데이터의 결측값을 채우는 방식이 아니라, 날짜 기준으로 주말 여부와 함께 `is_day_off` 파생 변수를 생성하는 방식으로 반영하였다.

---

### 4-6. 불필요 특성 제거

모델 학습 목적과 맞지 않거나 예측 시점에 알 수 없는 컬럼은 최종 입력 피처에서 제외하였다.

| 제거 대상 | 제거 이유 |
|----------|----------|
| `자전거번호` | 개별 자전거 식별자로 대여 수요 예측에 직접적인 의미가 낮음 |
| `대여 대여소명` | 문자열 정보이며 대여소 ID 및 위치 정보로 대체 가능 |
| `대여거치대` | 예측 시점에 안정적으로 활용하기 어려운 세부 정보 |
| `반납일시` | 대여 시점에는 알 수 없는 미래 정보이므로 데이터 누수 위험 |
| `반납대여소ID` | 대여 시점에는 알 수 없는 정보이므로 데이터 누수 위험 |
| `반납대여소명` | 반납 관련 정보로 최종 대여량 예측 기준에서 제외 |
| `반납거치대` | 대여 시점에는 알 수 없는 정보이므로 제외 |
| `이용시간(분)` | 대여 후에 알 수 있는 정보이므로 예측 시점에는 사용 불가 |
| `이용거리(M)` | 대여 후에 알 수 있는 정보이므로 예측 시점에는 사용 불가 |
| `생년`, `성별`, `이용자종류` | 개인정보성 또는 사용자 속성 정보이며, 대여소별 시간 단위 수요 예측 목적과 직접적인 관련성이 낮음 |
| `자전거구분` | 최종 대여소·시간 단위 수요 예측에서는 핵심 피처로 사용하지 않음 |

최종 모델 입력에는 예측 시점에 사전에 알 수 있거나, 과거 데이터로부터 계산 가능한 피처만 포함하였다.

※ 최종 모델은 트리 기반 회귀 모델을 중심으로 구성하였기 때문에 별도의 스케일링은 적용하지 않았다.

---

### 4-7. 최종 전처리 데이터 구성

전처리 후 최종 학습 데이터는 대여소, 날짜, 시간 단위의 수치형 피처와 타겟 변수로 구성하였다.

최종 전처리 데이터에서 확인된 주요 컬럼은 다음과 같다.

```text
station_id
latitude
longitude
rack_count
station_age_days
month
day
hour
dayofweek
is_day_off
hour_sin
hour_cos
temperature
humidity
wind_speed
precipitation
lag_1
lag_2
lag_3
lag_24
lag_48
lag_168
rolling_mean_3
rolling_mean_6
rolling_mean_24
rolling_std_24
diff_1
diff_24
rental_count
```

---

## 5. 데이터셋 분리

### 5-1. 분리 기준

본 프로젝트는 시간 흐름이 중요한 따릉이 대여량 예측 문제이므로 랜덤 분할을 사용하지 않았다.  
랜덤 분할을 사용할 경우 미래 시점의 데이터 패턴이 학습 데이터에 섞여 실제 예측 상황보다 성능이 과대평가될 수 있기 때문이다.

따라서 시간 순서를 유지하기 위해 연도 기준으로 데이터를 분리하였다.

| 구분 | 기간 | 용도 |
|------|------|------|
| Train | 2023년 | 모델 학습 |
| Validation | 2024년 | 모델 검증 및 하이퍼파라미터 튜닝 |
| Test | 2025년 | 최종 성능 평가 |

이와 같은 분리 방식은 과거 데이터를 기반으로 미래 수요를 예측하는 실제 서비스 상황과 유사하다.

### 5-2. 분리 결과

전처리 후 생성된 최종 데이터셋을 연도 기준으로 분리하였으며, 대용량 처리를 위해 parquet part 파일 형태로 저장하였다.

| 구분 | 기간 | 데이터 파일 | 용도 |
|------|------|------------|------|
| Train | 2023년 | `train_2023_part_*.parquet` | 모델 학습 |
| Validation | 2024년 | `valid_2024_part_*.parquet` | 모델 검증 |
| Test | 2025년 | `test_2025_part_*.parquet` | 최종 성능 평가 |

최종 전처리 데이터 기준 분리 결과는 다음과 같다.

| 구분 | 기간 | 행 수 | 타겟 합계 | 평균 대여량 | 0 대여량 비율 |
|------|------|------:|------:|------:|------:|
| Train | 2023년 | 23,459,991 | 44,282,784 | 1.888 | 0.451 |
| Validation | 2024년 | 23,818,357 | 43,494,512 | 1.826 | 0.455 |
| Test | 2025년 | 22,977,771 | 36,314,496 | 1.580 | 0.485 |

2025년 Test 데이터는 Train/Validation에 비해 평균 대여량이 낮고 0 대여량 비율이 높게 나타났다.  
따라서 최종 평가 시에는 전체 오차뿐 아니라 대여량이 낮거나 0인 구간에서의 예측 오차도 함께 고려할 필요가 있다고 판단하였다.

### 5-3. 최종 입력 특성 목록

최종 학습 데이터는 타겟 변수 `rental_count`를 제외한 수치형 피처를 모델 입력값으로 사용하였다.  
최종 입력 특성은 대여소 정보, 시간 정보, 쉬는 날 여부, 날씨 정보, 과거 대여량 기반 피처로 구성된다.

| 피처 그룹 | 최종 입력 특성 |
|-----------|----------------|
| 대여소 정보 | `station_id`, `latitude`, `longitude`, `rack_count`, `station_age_days` |
| 시간 정보 | `month`, `day`, `hour`, `dayofweek`, `hour_sin`, `hour_cos` |
| 쉬는 날 정보 | `is_day_off` |
| 날씨 정보 | `temperature`, `humidity`, `wind_speed`, `precipitation` |
| 과거 대여량 Lag 피처 | `lag_1`, `lag_2`, `lag_3`, `lag_24`, `lag_48`, `lag_168` |
| 이동 평균 피처 | `rolling_mean_3`, `rolling_mean_6`, `rolling_mean_24` |
| 변동성 피처 | `rolling_std_24` |
| 변화량 피처 | `diff_1`, `diff_24` |

| 항목 | 내용 |
|------|------|
| 최종 입력 특성 수 | 28개 |
| 타겟 변수 | `rental_count` |
| 최종 데이터 컬럼 수 | 29개 |
| 저장 형식 | parquet |

최종 모델 입력에는 예측 시점에 사전에 알 수 있는 정보 또는 과거 대여 이력으로부터 계산 가능한 정보만 포함하였다.  
반납일시, 반납대여소ID, 이용시간, 이용거리처럼 대여 이후에 알 수 있는 정보는 데이터 누수 방지를 위해 제외하였다.

---

## 6. 전처리 결과 요약

### 6-1. 전처리 전/후 데이터 비교

본 프로젝트에서는 원본 따릉이 대여 이력 데이터를 모델 학습에 바로 사용할 수 없었기 때문에, 대여소·날짜·시간 단위의 예측 데이터셋으로 변환하였다.

| 항목 | 전처리 전 | 전처리 후 |
|------|----------|----------|
| 데이터 단위 | 개별 자전거 대여 및 반납 기록 | 대여소 × 날짜 × 시간 단위 |
| 기준 시간 | `대여일시`, `반납일시` 모두 포함 | `대여일시` 기준 |
| 기준 대여소 | 대여소/반납대여소 정보 모두 포함 | `대여대여소ID` 기준 |
| 타겟 변수 | 별도 타겟 변수 없음 | 시간 단위 대여량 `rental_count` 생성 |
| 대여소 정보 | 원본 대여 이력과 분리되어 있음 | 대여소 ID 기준으로 위도, 경도, 거치대 수, 운영 기간 결합 |
| 날씨 정보 | 원본 대여 이력과 분리되어 있음 | 날짜·시간 기준으로 기온, 습도, 풍속, 강수량 결합 |
| 공휴일 정보 | 원본 대여 이력에 없음 | 주말 또는 공휴일 여부를 `is_day_off`로 생성 |
| 시간 정보 | 원본 일시 컬럼 형태 | `month`, `day`, `hour`, `dayofweek`, `hour_sin`, `hour_cos` 생성 |
| 과거 대여량 정보 | 없음 | `lag`, `rolling`, `diff` 피처 생성 |
| 반납/이용 후 정보 | 반납일시, 이용시간, 이용거리 포함 | 데이터 누수 방지를 위해 최종 입력 피처에서 제외 |
| 미매칭 대여소 | 일부 존재 | 최종 미매칭 대여소 58개 제거 |
| 저장 형식 | csv 원본 파일 | parquet part 파일 |

### 6-2. 전처리 파이프라인 요약

전체 전처리 과정은 다음 순서로 진행하였다.

```text
[원본 따릉이 대여 이력 데이터]
        │
        ├─ 월별/연도별 파일 단위로 데이터 로드
        ├─ 대여량 예측 목적에 맞게 대여 기준 컬럼 선택
        │   ├─ 기준 시간: 대여일시
        │   └─ 기준 대여소: 대여대여소ID
        │
        ├─ 대여일시 datetime 변환
        ├─ 날짜, 월, 일, 시간, 요일 정보 추출
        ├─ 대여소 × 날짜 × 시간 단위로 대여 건수 집계
        ├─ 대여가 없는 시간대 rental_count = 0 처리
        │
        ├─ 대여소 마스터 데이터 결합
        │   ├─ 위도, 경도, 거치대 수, 운영 기간 정보 결합
        │   └─ 끝까지 매칭되지 않는 대여소 58개 제거
        │
        ├─ 날씨 데이터 결합
        │   └─ 날짜·시간 기준으로 기온, 습도, 풍속, 강수량 결합
        │
        ├─ 주말/공휴일 정보 생성
        │   └─ is_day_off 생성
        │
        ├─ 시간 주기성 피처 생성
        │   └─ hour_sin, hour_cos 생성
        │
        ├─ 과거 대여량 기반 피처 생성
        │   ├─ lag_1, lag_2, lag_3
        │   ├─ lag_24, lag_48, lag_168
        │   ├─ rolling_mean_3, rolling_mean_6, rolling_mean_24
        │   ├─ rolling_std_24
        │   └─ diff_1, diff_24
        │
        ├─ 데이터 누수 위험 컬럼 제거
        │   ├─ 반납일시, 반납대여소ID
        │   ├─ 이용시간, 이용거리
        │   └─ 기타 예측 시점에 알 수 없는 정보
        │
        ├─ 연도 기준 데이터 분리
        │   ├─ Train: 2023년
        │   ├─ Validation: 2024년
        │   └─ Test: 2025년
        │
        └─ parquet part 파일로 저장

[최종 전처리 데이터] → 모델 학습 가능
```
### 6-3. 저장 파일 목록

최종 전처리 결과는 대용량 처리를 고려하여 parquet part 파일 형태로 저장하였다.

| 구분 | 저장 위치/파일명 | 설명 |
|------|------------------|------|
| Train 데이터 | `data/processed/train_2023_parts/train_2023_part_*.parquet` | 2023년 학습 데이터 |
| Validation 데이터 | `data/processed/valid_2024_parts/valid_2024_part_*.parquet` | 2024년 검증 데이터 |
| Test 데이터 | `data/processed/test_2025_parts/test_2025_part_*.parquet` | 2025년 최종 평가 데이터 |
| 최종 모델 입력 피처 | 모델 저장 객체의 `feature_cols` | 최종 입력 특성 목록 |
| 타겟 변수 | `rental_count` | 대여소·시간 단위 대여량 |

최종 전처리 결과는 대용량 처리를 고려하여 `data/processed/` 하위에 parquet part 파일 형태로 저장하였다.
### 6-4. 최종 전처리 데이터 구조

최종 전처리 데이터는 28개의 입력 피처와 1개의 타겟 변수로 구성된다.

| 항목 | 내용 |
|------|------|
| 최종 입력 특성 수 | 28개 |
| 타겟 변수 수 | 1개 |
| 최종 컬럼 수 | 29개 |
| 타겟 변수 | `rental_count` |
| 저장 형식 | parquet |
| Train 기간 | 2023년 |
| Validation 기간 | 2024년 |
| Test 기간 | 2025년 |

최종 전처리 데이터의 컬럼은 다음과 같다.

```text
station_id
latitude
longitude
rack_count
station_age_days
month
day
hour
dayofweek
is_day_off
hour_sin
hour_cos
temperature
humidity
wind_speed
precipitation
lag_1
lag_2
lag_3
lag_24
lag_48
lag_168
rolling_mean_3
rolling_mean_6
rolling_mean_24
rolling_std_24
diff_1
diff_24
rental_count
```
### 6-5. 전처리 결과 해석

전처리 결과, 원본 개별 대여 기록 데이터를 대여소·날짜·시간 단위의 회귀 예측 데이터셋으로 변환하였다.
최종 데이터는 시간 정보, 대여소 정보, 날씨 정보, 쉬는 날 여부, 과거 대여량 기반 피처를 포함하고 있으며, 모델이 대여소별 시간 단위 수요 패턴을 학습할 수 있도록 구성하였다.

또한 반납일시, 반납대여소ID, 이용시간, 이용거리처럼 예측 시점 이후에 알 수 있는 정보는 제외하여 데이터 누수를 방지하였다.
주말과 공휴일 여부는 별도 컬럼으로 분리하지 않고 `is_day_off` 하나로 통합하여 쉬는 날 여부를 반영하였다.

최종적으로 Train 2023, Validation 2024, Test 2025로 연도 기준 분리를 적용함으로써, 과거 데이터를 기반으로 미래 수요를 예측하는 실제 서비스 상황과 유사한 평가 구조를 구성하였다.

---

## 7. 한계점 및 향후 개선 방향

### 7-1. 한계점

본 프로젝트에서는 따릉이 대여 이력 데이터를 기반으로 대여소별 시간 단위 대여량 예측 데이터셋을 구축하였다.  
다만 데이터 전처리 및 모델 학습 데이터 구성 과정에서 다음과 같은 한계점이 존재한다.

| 한계점 | 설명 |
|------|------|
| 일부 대여소 정보 미매칭 | 대여 이력에는 존재하지만 대여소 마스터 데이터와 매칭되지 않는 대여소 ID가 일부 존재하였다. 최종적으로 58개 미매칭 대여소는 제거하였다. |
| 대여소 변경 이력 반영 한계 | 대여소 신설, 폐쇄, 이전, ID 변경과 같은 운영 이력을 완전히 반영하지 못했을 가능성이 있다. |
| 날씨 정보의 공간적 한계 | 날씨 데이터는 특정 관측 지점 또는 공통 기준의 기상 정보이므로, 개별 대여소 주변의 미세한 날씨 차이를 완전히 반영하지는 못한다. |
| 0 대여량 데이터 다수 | 모든 대여소에서 매시간 대여가 발생하는 것은 아니므로 `rental_count = 0`인 데이터가 많이 포함되어 있다. 이로 인해 낮은 수요 구간의 예측 난이도가 높을 수 있다. |
| 외부 환경 변수 부족 | 지하철역, 학교, 공원, 상권, 행사 정보 등 대여 수요에 영향을 줄 수 있는 추가 지리·환경 정보는 충분히 반영하지 못하였다. |
| 2026년 데이터 활용 한계 | 2026년 데이터는 기존 2023~2025년 학습 데이터와 컬럼 구조 및 대여소 매핑 기준이 완전히 동일하지 않아 공식 평가 데이터로 사용하지 않았다. |
| 관리자 서비스 관점의 데이터 한계 | 본 프로젝트는 대여 이력, 대여소 정보, 날씨 정보, 공휴일 정보를 중심으로 데이터를 구성하였다. 관리자 페이지에서 더 나은 의사결정을 지원하기 위해서는 실시간 재고, 재배치 이력, 고장/수리 정보, 대여소 혼잡도, 사용자 민원 등 운영 데이터가 추가로 필요하지만, 본 프로젝트에서는 해당 데이터를 충분히 반영하지 못하였다. |

### 7-2. 향후 개선 방향

향후에는 다음과 같은 방향으로 데이터 품질과 예측 성능을 개선할 수 있다.

| 개선 방향 | 설명 |
|------|------|
| 대여소 이력 관리 테이블 구축 | 대여소 신설, 폐쇄, 이전, ID 변경 이력을 관리하여 연도별 대여소 정보 불일치 문제를 줄일 수 있다. |
| 외부 지리 정보 추가 | 지하철역 거리, 학교·공원·상권 여부, 주거/업무 지역 구분 등을 추가하면 대여소별 수요 차이를 더 잘 설명할 수 있다. |
| 날씨 변수 고도화 | 체감온도, 미세먼지, 적설 여부, 강수 여부 등 추가 기상 변수를 반영하면 날씨에 따른 수요 변화를 더 세밀하게 학습할 수 있다. |
| 0 대여량 구간 별도 분석 | 대여 발생 여부를 먼저 예측한 뒤, 대여가 발생할 것으로 예상되는 구간에서 대여량을 예측하는 2단계 모델을 검토할 수 있다. |
| 최신 데이터 기반 재학습 | 2026년 이후 데이터의 구조를 기존 학습 데이터와 통일한 뒤, 모델을 재학습하여 최신 수요 패턴을 반영할 수 있다. |
| 예측 결과 모니터링 | 실제 서비스 적용 시 예측값과 실제 대여량의 차이를 지속적으로 기록하여 데이터 드리프트 여부를 확인할 필요가 있다. |
| 관리자 페이지용 운영 데이터 확대 | 관리자 페이지에서는 단순 예측값뿐 아니라 실시간 자전거 보유량, 거치 가능 수, 재배치 이력, 고장/수리 현황, 대여소별 혼잡도 등의 운영 데이터가 함께 제공될수록 더 나은 의사결정이 가능하다. 향후 이러한 데이터를 추가로 확보하면 예측 결과를 실제 운영 대응과 연결하는 서비스 완성도를 높일 수 있다. |

### 7-3. 최종 정리

본 전처리 과정에서는 원본 따릉이 대여 이력 데이터를 대여소·날짜·시간 단위의 회귀 예측 데이터셋으로 변환하였다.  
대여량 예측 목적에 맞게 `대여일시`와 `대여대여소ID`를 중심으로 데이터를 구성하였고, 대여소 정보, 날씨 정보, 쉬는 날 여부, 과거 대여량 기반 피처를 결합하였다.

또한 예측 시점 이후에 알 수 있는 반납 정보, 이용시간, 이용거리 등은 제외하여 데이터 누수를 방지하였다.  
최종 데이터셋은 Train 2023, Validation 2024, Test 2025로 시간 순서를 유지하여 분리하였으며, 이후 모델 학습 및 평가에 사용할 수 있는 형태로 저장하였다. <br>
본 프로젝트에서는 공개 데이터 기반으로 대여량 예측 데이터셋을 구축하였기 때문에, 실제 관리자 페이지에서 활용할 수 있는 운영 데이터에는 한계가 있었다.
향후 실시간 자전거 보유량, 거치 가능 수, 재배치 이력, 고장/수리 정보, 대여소별 혼잡도 등의 데이터가 추가된다면, 단순 예측을 넘어 운영자가 즉시 대응할 수 있는 관리자 서비스로 확장할 수 있다.


---
